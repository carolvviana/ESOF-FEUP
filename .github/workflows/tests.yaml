    name: test-my-app
    on: [push, workflow_dispatch]
    jobs:
      drive:
        runs-on: self-hosted
        steps:
        - uses: actions/checkout@v1
        - uses: subosito/flutter-action@v1
        - run: flutter test
